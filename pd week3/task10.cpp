#include <iostream>
using namespace std;
main()
{
    int num;
    int sum;
    sum = 0;
    cout << " Enter num1:";
    cin >> num;
    sum = sum + num;
    cout << " Enter num2:";
    cin >> num;
    sum = sum + num;
    cout << "Enter num3:";
    cin >> num;
    sum = sum + num;
    cout << " Enter num4:";
    cin >> num;
    sum = sum + num;
    cout << "Enter num5:";
    cin >> num;
    sum = sum + num;
    cout << "sum:" << sum;
}
